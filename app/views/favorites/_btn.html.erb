<% if recruitment.favorited_by?(current_user) %>
  <%= link_to recruitment_favorites_path(recruitment), method: :delete,  style: "color: red;" do %>
    <i class="fas fa-heart" aria-hidden="true"></i>
    <%= recruitment.favorites.count %>
  <% end %>
<% else %>
  <%= link_to recruitment_favorites_path(recruitment), method: :post do %>
    <i class="fas fa-heart" aria-hidden="true"></i>
    <%= recruitment.favorites.count %>
  <% end %>
<% end %>
